



"""
Django settings for config project.

Generated by 'django-admin startproject' using Django 6.0.2.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/6.0/ref/settings/
"""
import os
import dj_database_url

# On autorise Render à afficher ton site
ALLOWED_HOSTS = ['localhost', '127.0.0.1', '.onrender.com']
from pathlib import Path


# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/6.0/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '8KWj8QB8VcDHFbWddy-6RrMemVPyGHthFZZtfJe1Ew0SQLC9KwnPndgfGlv5zYlwVY4'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

DATA_UPLOAD_MAX_NUMBER_FIELDS = 2000

ALLOWED_HOSTS = ['*']


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',

    # mes modules :
    'core',
    'stock',
    'accounts',
    # Le nouveau package d'historique
    'simple_history',
   
    'patrimoine.apps.PatrimoineConfig',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'simple_history.middleware.HistoryRequestMiddleware',
    'accounts.middleware.AntiSpamMiddleware',
    'accounts.middleware.PasswordChangeMiddleware',  
    'accounts.middleware.SaaSMiddleware', 
    'accounts.middleware.ThemeMiddleware',
]

ROOT_URLCONF = 'config.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'templates'],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                
                'stock.context_processors.contexte_magasin',
                'stock.context_processors.notifications_erp',
                
                'stock.context_processors.validation_menu_context',
                'stock.context_processors.validation_demandes_menu',
                'stock.context_processors.entreprises_nav',
                'accounts.context_processors.theme_context',

            ],
        },
    },
]

WSGI_APPLICATION = 'config.wsgi.application'


# Database
# https://docs.djangoproject.com/en/6.0/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'chu_angre_db',     # Le nom exact qu'on a créé dans pgAdmin
        'USER': 'postgres',         # L'utilisateur par défaut
        'PASSWORD': 'admin', 
        'HOST': 'localhost',
        'PORT': '5432',
    }
}


# Password validation
# https://docs.djangoproject.com/en/6.0/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/6.0/topics/i18n/

LANGUAGE_CODE = 'fr'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/6.0/howto/static-files/

STATIC_URL = 'static/'

#AUTH_USER_MODEL = 'core.User'
# --- CONFIGURATION DE LA CONNEXION (Module accounts) ---
LOGIN_URL = '/auth/login/'           # Où aller pour se connecter
LOGIN_REDIRECT_URL = '/'             # Où aller UNE FOIS connecté (Tableau de bord)
LOGOUT_REDIRECT_URL = '/auth/login/' # Où aller après s'être déconnecté

# --- SÉCURITÉ DES SESSIONS ---
# Déconnecte l'utilisateur si on ferme la fenêtre du navigateur
SESSION_EXPIRE_AT_BROWSER_CLOSE = True 

# Déconnecte l'utilisateur après 20 minutes (1200 secondes) d'inactivité
SESSION_COOKIE_AGE = 1200               # 20 minutes = 1200 secondes
SESSION_EXPIRE_AT_BROWSER_CLOSE = False # Ne pas déconnecter à la fermeture du navigateur
SESSION_SAVE_EVERY_REQUEST = True       # 🔑 Rafraîchit le timer à chaque clic


# Autoriser l'affichage des fenêtres pop-up (iframes) sur le même site
X_FRAME_OPTIONS = 'SAMEORIGIN'

# URL publique pour accéder aux fichiers
MEDIA_URL = '/media/'

# Dossier physique où les fichiers sont stockés sur ton ordinateur
MEDIA_ROOT = BASE_DIR / 'media' 
# (ou MEDIA_ROOT = BASE_DIR / 'media' si tu utilises pathlib)

STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')
STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'

if 'DATABASE_URL' in os.environ:
    DATABASES['default'] = dj_database_url.config(conn_max_age=600, ssl_require=True)



AUTHENTICATION_BACKENDS = [
    'accounts.backends.TenantAwareBackend',          # ← Notre backend multi-tenant (prioritaire)
    'django.contrib.auth.backends.ModelBackend',     # ← Fallback Django standard
]

STATICFILES_DIRS = [
    BASE_DIR / 'static',
]
